class Solution {
public:
    int reverseDegree(string s) {
        int sum = 0;
        for(int i = 1; i <= s.length(); i++) {
            sum += (abs(s[i - 1] - 'a' - 26) * i);
        }
        return sum;
    }
};


OUTPUT:
Input: s = "abc"
Output: 148
